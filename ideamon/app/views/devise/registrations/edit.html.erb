<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>


<div class="well">          
  <div class="panel panel-info">
    <div class="panel-body">

      <div class="col-md-12">
        <div class="col-md-4" id="leftCol">
          <div class="well">  
            <div class="form-group">
              <%= f.label :username %><br />
              <%= f.text_field :username, class: "form-control" %>            
            </div> 
            <div class="form-group">
              <%= f.label :email %><br />
              <%= f.email_field :email, autofocus: true, class: "form-control" %>
            </div>
            <div class="form-group">
              <%= f.label :age %><br />
              <%= f.text_field :age, class: "form-control" %>
            </div>          
            <div class="form-group">
              <%= f.label :sex %><br />
              <%= f.select :sex, ["Weiblich", "Männlich"], {:prompt => "bitte wählen"}, class: "form-control"  %>
            </div>
          </div>
        </div>

        <div class="col-md-4" id="rightCol">
          <div class="well"> 
            <div class="form-group">
              <%= f.label :qualification %><br />
              <%= f.select :qualification, ["Universität/ Hochschule", "Abitur", "Fachhochulreife", "Mittlere Reife", "Hauptschulabschluss"], {:prompt => "bitte wählen"}, class: "form-control"  %>
            </div>
            <div class="form-group">
              <%= f.label :profession %><br />
              <%= f.text_field :profession, class: "form-control" %>
            </div>
            <div class="form-group">
              <%= f.label :nationality %><br />
              <%= f.text_field :nationality, class: "form-control" %>
            </div>
          </div>
        </div>

        <div class="col-md-4" id="rightCol">
          <div class="well"> 
            <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
            <% end %>

            <div><h3>Change Password</h3><i>(leave blank if you don't want to change it)</i></div>
            <div class="form-group">
              <%= f.label "new password" %><br />
              <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
            </div>
            <div class="form-group">
              <%= f.label :password_confirmation %><br />
              <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control" %>
            </div>
          </div>
        </div> 
      </div>   

      <div class="col-md-4 col-md-offset-4">
        <div class="form-group">
          <%= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
          <%= f.password_field :current_password, autocomplete: "off", class: "form-control" %>
        </div>
        <div class="actions">
          <%= f.submit "Update", class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
          
    </div>
  </div> 
</div>



<h3>Cancel my account</h3>

<p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

<%= link_to "Back", :back %>
